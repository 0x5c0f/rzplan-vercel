# AGENTS.md

## 项目定位

本仓库是从 `fastapi/full-stack-fastapi-template` 的后端部分提取并演化出来的项目。

- `rzplan` 表示“归零计划（Reset Zero Plan）”。
- `vercel` 表示当前仓库对 Vercel 部署做过适配与简化，但不代表后续所有新增能力都仍然适合部署在 Vercel 上。

本仓库的长期维护目标不是偏离上游模板自行演化，而是在保留上游基础架构的前提下，通过低耦合扩展承载项目需求。后续开发默认要服务于这个目标。

## 初始化仓库边界

本仓库应被视为一个初始化仓库，而不是完整业务仓库。

在默认状态下：

- `rz` 业务范围仅限字典模块
- 不要新增任何非字典类 `rz` 业务能力
- 不要因为其他来源存在额外能力，就默认把它们同步到当前仓库
- MCP 只保留最小接入与清单能力，不作为继续扩展工具能力的默认入口

如果需要突破这个边界，必须由维护者明确确认，而不是由开发者或 AI 自行推断。

## 总原则

### 1. 先理解，再修改

在修复问题、重构代码或新增功能前，先理解现有实现与扩展边界。不要在没有建立清晰项目认知前直接写代码。

### 2. 先定义成功标准，再开始实现

对修 bug、加接口、补校验、做重构这类任务，不要只按口头要求直接展开实现。

先明确什么结果算完成，优先把任务转成可验证的目标，例如：

- 问题是否可以稳定复现
- 新增行为应由什么测试或检查证明成立
- 改动后哪些已有行为必须保持不变

如果成功标准说不清，应先澄清需求，再进入实现。

### 3. 默认最小改动

无明确要求时：

- 不升级依赖
- 不替换框架
- 不重组目录
- 不重写模板原有逻辑

优先做小范围、接缝式、可回退的改动。

判断一处改动是否合理时，默认要求它能直接追溯到当前需求；如果一处修改无法明确说明与本次任务的关系，就不要顺手改它。

### 4. 默认优先扩展，不优先侵入

新增业务能力时，优先在 `rz` 命名空间下扩展，而不是直接改模板原生代码。

目标是尽量保持与上游 `fastapi/full-stack-fastapi-template` 的低耦合，便于维护者后续手动同步上游更新。

### 5. 不要把模板占位实现默认当作 bug

本仓库保留了部分模板或占位性质的实现。除非需求明确要求，否则不要主动把这些实现视为待修复缺陷，也不要擅自补全。

例如：

- 某些模板接口可能只是保留形状，不承担完整业务能力
- 某些默认配置或示例实现只是为了保留模板结构

## 代码区域划分

后续开发时，先判断改动属于哪个区域，再决定落点。

### 模板原生区

这些区域主要继承自上游模板，默认谨慎修改：

- `app/models.py`
- `app/crud.py`
- `app/api/routes/*.py` 中的模板原生路由
- `app/core/*`
- `scripts/*`
- 其他明显属于模板基础设施的文件

### 模板接缝区

这些位置允许做少量接线型修改，用于把 `rz` 扩展挂接到现有应用：

- `app/main.py`
- `app/api/main.py`
- `app/core/config.py`
- `app/alembic/versions/*`
- 启动、部署、迁移、注册相关接入点

这类修改应该尽量保持“小而明确”，只做注册、配置、挂载、集成，不要在这里堆积业务逻辑。

### RZ 扩展区

新增项目能力默认优先落在这里：

- `app/rz/models`
- `app/rz/crud`
- `app/rz/services`
- `app/rz/subsystem`
- `app/rz/utils`
- `app/api/routes/rz`
- `app/tests/rz`

如果新增能力无法自然落在 `rz` 区域，需要先判断是否真的属于模板基础能力，而不是习惯性把代码写回模板区。

## 目录与扩展规范

`rz` 是本仓库的默认业务扩展命名空间。后续新增能力应尽量沿用现有字典模块的组织方式。

推荐结构如下：

- `app/rz/models`
  - 放 SQLModel 模型、领域数据结构、RZ 专属接口模型
- `app/rz/crud`
  - 放直接面向数据库的增删改查逻辑
- `app/rz/services`
  - 放跨模型、多步骤、带业务编排性质的逻辑
- `app/rz/subsystem`
  - 放类似 MCP、系统集成、子系统注册、协议暴露等能力
- `app/rz/utils`
  - 放仅在 RZ 领域内复用的工具函数
- `app/api/routes/rz`
  - 放 RZ 专属 API 路由
- `app/tests/rz`
  - 放 RZ 扩展的测试

如果当前需求较简单，不需要为了“结构完整”强行创建所有目录。可以按需添加，但目录职责应保持清晰。

## 模板区允许修改的例外情况

以下场景允许修改模板区，但仍应保持最小化：

### 1. 路由注册与应用挂载

例如：

- 在 `app/api/main.py` 注册新的 `rz` 路由
- 在 `app/main.py` 挂载新的子系统或中间件

### 2. 配置项接入

例如：

- 在 `app/core/config.py` 增加新功能必须的环境变量
- 为 `rz` 扩展补充配置映射

### 3. 数据库接入点变更

例如：

- 新增模型后补充 Alembic 迁移
- 在必要情况下调整数据库初始化接线

### 4. 启动与部署接缝

例如：

- 修改启动脚本以接入新扩展所需的初始化步骤
- 为部署环境补充必要接线

### 5. 认证、依赖注入、全局基础设施的最小接入

如果新能力必须接入现有认证、依赖、日志、启动流程，可以修改模板区，但优先做接线式改动，不要顺手重写基础层。

## 代码开发规范

以下规范不是通用 Python 教程，而是本仓库的实现约束。

### 1. 分层职责要稳定

- 路由层负责参数接收、权限校验、调用下层、返回响应
- `crud` 层负责直接数据库操作
- `services` 层负责复杂业务编排
- `subsystem` 层负责系统级集成能力
- `models` 层负责数据结构和模型定义

不要把复杂业务逻辑直接堆进路由，也不要把系统集成逻辑塞进普通 CRUD。

### 2. 优先把可复用校验放进依赖

FastAPI 的 dependency 不仅用于依赖注入，也适合承载可复用的请求级校验。

例如：

- 资源是否存在
- 当前用户是否有权访问某个对象
- 某类输入是否满足数据库约束

如果同一类校验会在多个路由重复出现，优先抽成 dependency，而不是在每个路由里重复编写。

### 3. 优先参考现有 RZ 模块

字典模块是当前仓库最主要的 `rz` 参考实现。

MCP 子系统在当前仓库中只保留最小接入结构，可以参考其挂接方式，但不应据此继续扩展新的工具能力。

### 4. 命名与风格保持一致

- 延续现有文件命名、模块组织、错误处理和返回风格
- 不为单一需求引入另一套命名体系
- 注释只在必要时添加，避免无效解释

### 5. 无明确必要，不新增依赖与抽象层

- 不要为了“更优雅”引入新的第三方库
- 不要为了“以后可能会用到”提前设计复杂抽象
- 优先复用模板已有机制和项目已有实现

### 6. 外部 I/O 优先使用异步边界

如果新能力涉及：

- 外部 HTTP 请求
- 第三方 API 调用
- 其他明显的 I/O 密集型操作

优先选择异步方案，并把调用放在清晰的边界层中。

如果必须使用同步 SDK：

- 不要把阻塞调用直接堆在路由主流程里
- 先明确它属于哪个边界层，再决定如何隔离阻塞影响

### 7. 数据库变更必须成套考虑

涉及模型调整时，必须同时考虑：

- Alembic 迁移是否需要新增或调整
- 现有查询与删除逻辑是否受影响
- 测试是否需要补充

Alembic 迁移应尽量满足：

- 迁移意图清晰可读
- 结构变更保持静态，不依赖运行时状态
- 在合理范围内可回滚

不要把临时脚本式逻辑塞进结构迁移里。

### 8. 测试应跟随新增行为

新增或修改 `rz` 业务能力时，优先在 `app/tests/rz` 中补对应测试。

如果改动触及模板区接缝，也应补最接近该行为的测试，而不是只改代码不验证。

## 对 AI 代理的执行要求

## 如何判断规范正在生效

如果后续开发基本符合以下现象，说明本规范正在发挥作用：

- 改动默认集中在 `rz` 区域，以及少量必要的模板接缝文件
- 非字典类需求不会被默认实现在当前仓库
- 修改模板区前，会先说明改动原因与影响范围
- 新增或变更行为时，通常会补最接近该行为的验证或测试
- 最终 diff 以当前需求直接相关内容为主，而不是伴随大范围顺手整理

## 对 AI 代理的执行要求

后续 AI 代理在本仓库工作时，默认遵守以下要求：

### 1. 先判断是否应该放进 `rz`

遇到新需求时，先判断：

- 这是模板基础能力的修补
- 还是项目业务扩展

如果是业务扩展，先确认它是否仍在“初始化仓库默认仅保留字典业务”的边界内。

只有字典相关扩展，才默认继续放进 `rz` 并在当前仓库实现。

### 2. 修改模板区前先说明原因

如果必须修改模板原生文件，应先说明：

- 为什么不能只在 `rz` 中完成
- 修改属于“接线型修改”还是“行为型修改”
- 对上游同步的潜在影响是什么
- 这次修改准备如何验证生效且不破坏现有行为

### 3. 不要主动做大范围“顺手优化”

除非明确要求，否则不要顺手：

- 重构模板层
- 统一风格到另一套规范
- 批量改名
- 搬迁目录
- 补完模板占位能力
- 补入其他来源里的额外功能线
- 在 MCP 或其他接缝上继续长出非字典能力

### 4. 保持沟通语言清晰

与维护者沟通时默认使用简体中文。

代码、注释、字段命名则优先遵循所在文件已有语言和风格，而不是强行全部中文化。

## 设计目标总结

本仓库的开发默认目标只有三个：

1. 新需求尽量在 `rz` 命名空间内完成
2. 与上游模板保持低耦合
3. 在必须修改模板区时，只做清晰、最小、可解释的接缝式改动
